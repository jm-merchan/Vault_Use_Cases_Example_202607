apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: vm-monitoring
  annotations: {}
spec:
  internalTrafficPolicy: Cluster
  ports:
  - port: 9090
    protocol: TCP
    targetPort: 9090
  selector:
    app: vm-prometheus
  sessionAffinity: None
  type: ClusterIP
