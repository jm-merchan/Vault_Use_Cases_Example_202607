apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: vm-monitoring
  annotations: {}
spec:
  internalTrafficPolicy: Cluster
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: vm-grafana
  sessionAffinity: None
  type: ClusterIP
